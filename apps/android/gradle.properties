org.gradle.daemon=false
org.gradle.caching=false
org.gradle.parallel=false
kotlin.code.style=official

# This controls whether the SDK-backed source set participates in the build.
# It is deliberately false unless a reviewer opts in on a machine with an SDK.
inkshadow.includeAndroidSdkModule=false
