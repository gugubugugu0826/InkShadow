# InkShadow 0.2.12 复测缺陷修复与发布记录

> 本文只记录当前源码重新取得的证据。两份 0.2.11 真实安装报告属于现场线索；没有随报告提供的数据库、诊断包、请求收据、调用栈和截图原件均标记为“未取得”。本轮不执行新的真实付费模型调用。
> 跟踪边界：本专项记录已由 `.gitignore` 精确放行，将随本轮候选提交纳入跟踪。本文冻结最终本地门禁完成时的源码快照；唯一候选提交、推送、标签、安装包和 Release 仍由后续候选与发布证据确认，不在源码文档中提前写成成功。

## 一、证据边界

- 被测 0.2.11 安装包：`InkShadow_0.2.11_x64-setup.exe`，7,750,596 字节，SHA-256 `4dabb98602c45b5fe49b822cddf23f927bc03c7963ed1ab5b260ccb86d56c28f`；仓库内候选包、来源清单和验证记录保持原样。
- 第一轮报告中的 K01–K12 未完整附带或逐项映射；补充报告声明 L01–L10，但现场目录只到 L09。P1-1、P2-1、P2-3 和 P3 项缺少能够独立复核的完整交互证据。
- 补充报告称调用账本共 5 笔，但分类写成“能力验证 2 + 开书 3 + 续写 1”，合计为 6；逐笔调用标识、供应商收据、时间和额度明细未取得，因此不改写为确定事实。
- 0.2.11 的旧库升级、三方案开书、候选隔离、真实续写、设定整理、私密章节零发送、零自动重试和重启恢复，作为历史现场防回归线索保留；当前提交仍须重新执行自动化与本地门禁。

## 二、缺陷—源码—验证矩阵

| 报告缺陷                    | 优先级    | 证据冲突与准确根因                                                                                                                                                                                                                                                                                                                                        | 修复前回归                                                                                                                    | 当前修复                                                                                                                                                                                                                                     | 当前已取得验证                                                                                                                                    | 提交状态             |
| --------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| 顶栏与模型中心连接状态矛盾  | P1-1      | 报告文字称同一连接在不同入口同时显示可用与失败，但 K04 没有同时拍到该矛盾，现场状态快照与初始化时间线未取得。源码审计确认真实根因：设置页、顶栏和快速连接抽屉分别读取并投影连接状态；精确读取失败还能用空集合覆盖已水合结果，刷新期间也没有统一撤销可发送判断；不可信凭据引用还会在异步隔离建立前同步抛出，使首次深链错误退化为“连接失败、已保存连接 0”。 | 增加共享投影、刷新暂时撤销可发送状态、失败保留上一份计数但关闭发送，以及可信、缺失、不可读取、不可信和混合凭据状态回归。      | 新增单例权威状态仓库与订阅钩子；三处界面读取同一快照。每次刷新先发布“正在核对”，连接、凭据、目录、路线或能力不完整时统一关闭可发送判断；单条不可信凭据隔离为“凭据来源需要重新保存”，保留已保存连接且零发送；失败提供支持编号和重新读取入口。 | 连接状态与设置聚焦 6 个文件、117 项通过，0 项失败、0 项跳过；不可信凭据修复前 1 项稳定失败，修复后核心 3 个文件 19／19 通过。                     | 已纳入本轮待提交范围 |
| 设定证据展开                | P2-1      | 两轮报告并不互相证明：第一轮只记录 WebDriver 合成点击无响应，第二轮只证明设定数据存在；点击前后录像、DOM 事件、焦点轨迹和真人视觉均未取得，不能把现场工具问题或产品问题任一方写成既定事实。当前源码审计发现可补强的真实合同缺口：人物详情未公开完整对话框关系，程序化激活后没有显式恢复触发点焦点，正式浏览器用例也没有覆盖全部标准激活方式。             | 为人物详情、原文依据和正式证据补充鼠标、Enter、Space、触摸、程序化点击、展开状态、受控区域、Escape 和焦点返回回归。           | 证据按钮显式保持触发点焦点；人物详情增加按钮、对话框、展开状态和受控区域关系；关闭后返回原入口。正式浏览器用例覆盖五种激活路径。                                                                                                             | 当前组合聚焦 4 个文件、49 项通过，0 项失败、0 项跳过；正式网页端到端最终 21／21 通过。真人视觉与真实触摸设备未执行。                              | 已纳入本轮待提交范围 |
| 开头/续写名称漂移与私密入口 | P2-2      | 报告截图与文字对按钮名称不一致，现场章节隐私、版本、任务和调用标识未取得。源码根因是生成计划、Model Hub 任务、上下文记录和部分用量关联曾硬编码为续写，界面又存在重复入口并可能从候选或草稿推断动作，导致空正文显示名和持久任务身份漂移。                                                                                                                  | 增加空白/非空权威正文映射、任务元数据、上下文与用量身份、重启恢复、候选隔离、任务中心中文名称和私密章节关闭入口回归。         | 从已保存权威正文确定 `prose_generation`“生成开头”或 `continuation`“生成续写建议”，贯穿计划、披露、任务、调用、上下文、候选、用量与恢复链；移除重复生产入口。私密章节关闭远程选区操作。                                                       | 动作身份、创作链与用量聚焦 4 个文件、51 项通过；候选路由 67 项通过；任务中心、进度、应用和重入组合 5 个文件、46 项通过。均为 0 失败、0 跳过。     | 已纳入本轮待提交范围 |
| 设置页导入与导出跳转        | P2-3      | 报告称点击后仍在外观区，但补充报告 L09 已显示导入导出区，所引 L10 不存在；浏览历史与焦点证据未取得。源码审计确认深链合同缺口：快速入口未公开当前位置，目标焦点落在泛化容器，目标延迟挂载时缺少稳定恢复。                                                                                                                                                  | 增加目录激活、直接深链、刷新恢复、前进后退、目标标题焦点及未挂载重试回归。                                                    | 快速入口统一由哈希深链生成并公开当前位置；滚动后聚焦可理解标题或首个可操作控件；目标未挂载时保留地址并可重试。新增正式浏览器深链用例。                                                                                                       | 设置页包含在连接状态聚焦 6 个文件、117 项通过；正式网页端到端最终 21／21 通过。应用内浏览器未能启动，不能写成人工通过。                           | 已纳入本轮待提交范围 |
| 原生四格式导出落盘          | P2-4      | 报告只证明入口可见；WebDriver 无法操作原生保存对话框，实体文件、哈希和外部打开结果未取得。当前审计未发现需要修改生产导出实现的缺陷。                                                                                                                                                                                                                      | 复跑导入导出、桌面导出、原生导出、浏览器下载解析、备份恢复和数据守恒合同。                                                    | 没有为表面通过修改生产逻辑；继续保留格式、扩展名、媒体类型、字节、保存票据、取消与失败恢复合同。                                                                                                                                             | 导入导出 91 项、桌面导出 24 项、原生导出 9 项、浏览器下载解析 1 项均通过，0 项失败。原生保存对话框、真实实体文件外部打开和真人操作未执行。        | 已纳入本轮待提交范围 |
| 扩写、改写、润色、缩写      | P3-1/P3-3 | 当前生产链原本具备四种动作，但入口只在已有选区时出现，导致不可发现；选区整理记录又曾把任务身份固定为改写，润色审计不准确。报告只检查局部视口，未取得完整界面和调用材料。                                                                                                                                                                                  | 增加直接/专业模式无选区可见、精确选区、四动作名称与底层任务、披露取消、隔离候选、接受新版本、派生失败不回滚、私密零发送回归。 | 两种写作模式始终显示同一组选区操作；没有选区时说明原因并禁用。选中后明确范围、模型、服务、资料和隐私去向；四种结果均先隔离，接受后才创建新版本。整理记录使用真实动作任务身份，私密章节全部关闭。                                             | 当前组合聚焦 4 个文件、49 项通过；候选路由 67 项通过；相关动作身份、创作链与用量 51 项通过。均为 0 失败、0 跳过。没有执行真实供应商输出质量验证。 | 已纳入本轮待提交范围 |
| 测试作品标记                | P3-2      | 报告只看到提示，没有执行标记、取消或重启，现场内容摘要未取得。当前源码已有完整显式身份链，问题是缺少把全行为串联起来的单一合同证据，而不是无效果入口；名称和正文从不参与分类。                                                                                                                                                                            | 新增“作者作品→标记测试作品→重启→取消标记”合同，同时逐步对比作品、正文、不可变版本、隔离候选和导出摘要。                       | 保留既有可持久化、可撤销且带修订历史的分类实现；新增独立完整合同测试，没有修改生产代码或迁移。                                                                                                                                               | 数据层 3 个文件、9 项通过；桌面端 3 个文件、29 项通过；均为 0 失败、0 跳过。真实安装程序重启未执行。                                              | 已纳入本轮待提交范围 |

## 三、最终本地门禁

以下结果均来自当前 0.2.12 工作树。聚焦分组彼此重叠，不与全仓总数相加；可控网关也不等于真实供应商验证。

| 命令或范围                                             | 当前结果                                                              | 边界                                                                                            |
| ------------------------------------------------------ | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `pnpm test`                                            | 509 个测试文件通过、16 个文件跳过；3,870 项通过、65 项跳过、0 项失败  | Desktop 为 292 个文件、2,495 项通过、1 项跳过                                                   |
| `pnpm typecheck`                                       | 20／21 个工作区通过                                                   | 仅 20 个工作区提供对应脚本                                                                      |
| `pnpm lint` / `pnpm format:check`                      | 全部通过                                                              | 0 个规范或格式错误                                                                              |
| `pnpm test:scripts`                                    | 39 项通过                                                             | 0 项失败、0 项跳过                                                                              |
| `pnpm check:secrets` / `pnpm check:licenses`           | 敏感信息扫描通过；137 项运行时许可通过                                | 不包含真实凭据读取                                                                              |
| `pnpm check:boundaries` / `pnpm check:desktop-release` | 20 个包边界通过；发布配置通过                                         | 只验证当前源码与配置                                                                            |
| `pnpm build`                                           | 20／21 个含构建脚本的工作区通过                                       | Desktop 2,306 个模块、27 个分块；总量 7,335,567／7,340,032 字节；最大分块 489,186／512,000 字节 |
| `pnpm check:rust`                                      | 207 项通过、0 项失败、1 项忽略                                        | `cargo fmt` 与严格 `clippy -D warnings` 通过                                                    |
| `pnpm test:e2e`                                        | 最终 21／21 通过                                                      | 首轮 19／21，修复旧重复入口定位后两条分别 1／1，完整复跑 21／21                                 |
| `node scripts/check-visual-evidence.mjs`               | 40 条记录、40 个唯一 PNG 通过                                         | 与端到端退出 0 共同证明浅色、深色、目标宽度和等效 200% 矩阵；不等于真人视觉复核                 |
| 浏览器下载解析聚焦                                     | 1／1 通过                                                             | 验证浏览器下载字节和重新解析；不替代原生保存对话框                                              |
| 数据层全量                                             | 81 个文件、478 项通过                                                 | 0 项失败、0 项跳过                                                                              |
| 连续升级与原生迁移                                     | 2 个文件、2 项连续升级通过；原生迁移 18 项通过                        | 覆盖 Data `0078` / Tauri `81`，不等于现场旧库真机升级                                           |
| 备份恢复                                               | Desktop 31 项、原生层 19 项通过                                       | 不替代安装程序真机恢复                                                                          |
| 导入导出                                               | 导入导出 91 项、Desktop 导出 24 项、原生导出 9 项通过                 | 原生保存对话框和外部程序打开未人工执行                                                          |
| 私密章节与创作状态链                                   | 私密章节筛选 12 项通过、148 项因名称筛选未运行；创作状态链 160 项通过 | 没有真实付费调用                                                                                |

### 首次失败与修复链

- 首次 `pnpm test` 的 Desktop 结果为 290 个文件通过、2 个文件失败；2,485 项通过、2 项失败、1 项跳过。一个普通界面合同仍查找固定 JSX 文本，没有适配数据驱动的“缩写”按钮；另一个把关闭和取消合在同一长用例中，在全量负载下于 5.023 秒超时。修复只更新真实合同断言并拆分两个独立零发送场景，没有放宽生产门禁或全局超时；聚焦 71／71、两条独立场景各 1／1，最终全仓通过。
- 正式构建首次为 7,344,006／7,340,032 字节，超出固定预算 3,974 字节。没有提高预算；关闭不必要的模块预加载并压缩等价 SQL 字面量后，相关存储和生成链回归分别 30／30、98／98 通过；两条提交前阻断修复纳入后，最终为 7,335,567 字节，余量 4,465 字节。
- 首次 `pnpm check:rust` 的格式与严格静态检查通过，测试为 206 项通过、1 项失败、1 项忽略。唯一失败是 Tauri 迁移上限前移到 81 后，迁移校验诊断测试仍期待 80；把测试期望同步为真实上限 81 后，聚焦复跑和完整原生门禁通过，最终为 207／0／1。
- 正式网页端到端首轮为 19／21；两项失败来自旧重复入口定位，没有触发产品断言。定位修正后两条分别 1／1，通过后完整复跑 21／21。
- 应用内浏览器交互复核两次均在打开页面前被 Windows 沙箱辅助进程 `helper_unknown_error` 阻断，未取得页面或截图证据；该失败不被改写为真人视觉或应用内浏览器通过。
- 提交前差异审计又稳定复现两条发布阻断：不可信凭据引用在权威装载中同步逃逸，以及 0.2.11 旧延迟／重试任务缺少 `modelTask` 时只从本次计划忽略、没有持久化终结。前者修复前新增回归 1 项失败，修复后核心 3 个文件 19／19 通过；后者修复前真实本地持久化与重启回归 4／4 失败，修复后 4／4、完整生成运行时 38／38 通过。旧记录分别进入可审计的安全终态，关联任务取消，新计划使用新标识，准备阶段发送 0 次，正文、不可变版本和候选摘要不变。
- 两项迟发现修复后，连接／设置组合 6 个文件 117／117、联合回归 5 个文件 120／120、最终全仓 3,870 项通过；类型、规范和格式检查再次通过。

### 数据守恒

- 40,936 字夹具的作品、章节、2 个不可变版本、恢复稿、灵感、正式设定、隔离候选、任务和运行记录在迁移前后有序摘要一致。
- 原生大型夹具包含 14 个作品、19 个章节、36 个不可变版本、18 个候选、约 115,000 字、190 张用户表和 81 条迁移回执；升级、备份恢复及重启前后摘要一致。
- 这些均为构造夹具，不是两份报告的现场数据库；不能外推为真实旧库覆盖安装已经执行。

## 四、未取得与未执行

- 未取得：现场数据库、完整诊断包、逐笔调用标识和供应商收据、原始状态快照、完整截图集、调用栈、原生导出实体文件，以及可独立复核 P2-1 点击前后状态的录像或焦点轨迹。
- P2-1 冲突判定：第一轮的合成点击无响应和第二轮的数据存在只能证明不同事实，均不足以证明或排除真实交互缺陷；当前源码已补强语义、多输入方式和正式网页端到端回归，但真人视觉未执行。
- P2-4 未人工执行：未操作原生保存对话框，未取得四格式实体文件，未在外部程序打开核验。
- 未执行新的真实 DeepSeek 或其他付费模型调用；当前模型相关结果均来自可控本地测试，不得写成真实供应商验证。
- 未执行真人视觉、真实触摸设备和 Windows 真实百分之二百系统缩放；应用内浏览器两次启动失败，未取得替代人工证据。
- 最终本地源码、原生与网页端到端门禁已完成；唯一干净候选提交、远端 `main` 同步、安装包、标签、GitHub Release、附件上传和回下载复核仍由后续发布链执行。本文是源码快照，不得被解释为这些外部动作已经完成。

## 五、后续候选与发布字段

后续只有在同一干净提交的候选链再次通过后，才能记录发布提交、来源指纹、安装程序版本与签名状态、标签解析提交、五个附件的大小与 SHA-256、GitHub Actions、Release 链接和回下载复核。精确结果写入发布附件中的中文验证报告；源码文档不预填未知提交或制品值，也不通过发布后的追加提交使远端 `main` 偏离标签来源。
