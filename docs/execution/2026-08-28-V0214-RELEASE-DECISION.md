# InkShadow 0.2.14 发布决定记录

- 决定日期：2026-08-28
- 决定基线：本文件所在的最终发布提交
- 发布形式：GitHub 预发行
- 安装程序状态：未签名

## 决定

最新用户指令已明确授权将 InkShadow 0.2.14 推送到 `main`，并以 GitHub 预发行形式提供。该授权取代此前“仅形成候选、等待单独发布指令”的流程限制，但不改变既有质量证据的含义，也不允许把未执行的验证描述为已经完成。

0.2.14 必须以“未签名工程预览”发布。README、发布说明和下载页应明确提醒 Windows 可能显示未知发布者或安全提示，用户应仅从项目的 GitHub Release 页面下载，并在安装前核对随附的 SHA-256 校验值。

最终发布源重验曾在 `ba5652e38a49ad691b6cc4e4f32f79ea32628c41` 的四万字证据性能门禁处失败关闭。该失败没有被重跑掩盖：发布源通过新的向前迁移 `0081` 保持原有精确校验并消除重复扫描，随后必须在新的唯一提交上从头完成全部候选门禁。此前 `b4ec54f` 的成功候选结果不能替代新的同源证据。

提交 `bd7e3cb96c970eb7c0bba53944401ee8dbcafc04` 随后完成本地同源候选并安全推送到 `main`，但 GitHub 运行 `33101514463` 的质量作业在 Windows 文件型 SQLite 维护测试处失败。首因是三条完整付费权威链恢复中的 `running` 场景超过通用 15 秒测试基础设施上限；超时后异步操作继续占用共享备份文件，导致 `EPERM` 和下一场景覆盖拒绝级联。该提交没有创建标签或 Release。当前稳定化只给这三条完整恢复场景沿用既有 30 秒专项标准，并隔离临时路径、保证关闭后清理；产品备份实现、迁移和所有恢复后断言均未放宽。新的唯一提交必须重新完成本地候选与三项远端门禁。

第二个同源提交 `400120f643f9fccfe929560a39e79f263a384b1d` 也完成本地候选并安全推送，但 GitHub 运行 `33107276512` 的质量作业在桌面全量测试发现两项测试基础设施问题；云数据库与 Windows 原生作业均成功。固定验证辅助函数没有等待异步发送说明准备完毕，测试在产品正确的防重复忙碌态同步寻找确认按钮。版本恢复夹具则把 84 份完整迁移同步写入 Windows 临时存储；结合同步实现与修复前后对照，判定该路径是主要耗时来源，该测试文件总耗时 107.674 秒。该提交同样没有创建标签或 Release。修复只为测试建立有界说明等待，并将完整迁移先在内存执行后一次性物化真实文件；生产发送、恢复、正文和版本逻辑、确认前零发送、真实关闭重开、数据守恒断言及 20 秒上限均未放宽。新的唯一提交仍须重新完成全部本地候选和三项远端门禁。

## 已取得的自动化证据

- 界面合同测试恢复后：6 个文件、42 项通过。
- 全仓测试：514 个文件通过、16 个文件跳过；3,945 项通过、65 项跳过、0 失败。
- 桌面端测试：296 个文件、2,554 项通过、1 项跳过。
- 数据层测试：481 项通过；故事核心测试：148 项通过。
- 原生层格式和严格静态检查通过；测试 207 项通过、0 失败、1 项忽略。
- 正式桌面构建通过；普通网页端到端旅程 26/26 通过。

上述完整候选结果绑定第二个同源提交 `400120f643f9fccfe929560a39e79f263a384b1d` 及其之前的同源运行。当前测试稳定化工作区已另行通过四组直接回归 124／124、SQLite 重开连续 10／10、应用层版本恢复 7／7、数据仓储与适配器 52／52、桌面端完整测试 2,554 项通过且 1 项跳过，以及全仓类型、代码规范和格式检查；新的唯一提交仍须重新执行完整候选链。这些自动化结果不等同于真实服务、真实安装环境或真人体验已经完成验证。

## 仍未执行或未取得的证据

- 未执行真实付费模型调用和真实服务商长响应验证。
- 未执行真人视觉、键盘、触摸及完整人工创作流程复核。
- 未执行 0.2.14 安装程序的真实安装、升级、卸载、重装和旧数据恢复验证。
- 未执行真实 Windows 系统 200% 缩放验证；浏览器等效缩放不能替代该证据。
- 三份原始测试报告原件以及 Q01–Q11、P02–P10、R05–R14 现场证据未取得。

上述缺口必须继续如实保留在发布说明和验证报告中，不得改写为“已验证”。本次授权允许以预发行形式提供安装包供人工验证，不代表将 0.2.14 宣称为经过全部真实环境验收的稳定正式版。

## 发布保护要求

1. 标签、Release、安装程序、来源清单和校验文件必须指向同一个干净提交。
2. 不得覆盖或移动既有标签和发布附件，不得强制推送。
3. Release 必须标记为预发行，并明确安装程序未签名。
4. 发布附件应包含来源清单、SHA-256 校验文件和当前验证报告。
5. 发布后应从 GitHub 重新下载附件，复核名称、大小和 SHA-256。
6. 备份、恢复和本地数据保护仍按现有安全合同执行；发布授权不放宽正文、版本、灵感、设定、历史生成结果和凭据的保护要求。

## 结论

0.2.14 已从“等待发布授权”转为“允许在完成干净来源门禁和附件核验后发布未签名预发行”。远端推送、标签和 Release 操作由获授权的发布流程执行，本记录本身不证明这些远端动作已经完成。
