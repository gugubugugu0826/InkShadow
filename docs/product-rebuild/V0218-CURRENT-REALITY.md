# 0.2.18 当前实现与验收边界

修复工作区完整检查通过后，应用版本已由 0.2.17 更新为 0.2.18；最终干净提交仍须完整执行候选构建链，不能用更新版本代替发布验收。

本轮继续复用事实仓储、不可变版本、调用记录、技能绑定和作者恢复记录，不创建平行权威存储，不修改已发布迁移。逐句草稿新增属性、状态、能力与事件分类，只有作者确认后才能成为正式设定。

隐私和规则锁定增加提交后的权威回读。故障注入验证不等于取得现场数据库，真实测试中的状态矛盾仍须与现场材料区分。

规划模型推荐修正缺失任务的筛选条件；文字生成成功与规划格式能力验证保持分离。自定义技能保留作者规则进入请求，不宣称真实文章已经体现预期效果。

待确认设定可以进入辅助核对列表，但不计作已确认事实或锁定规则。显示正文依据前必须核对不可变版本、精确范围与原文；不能核对时明确缺失原因。技能采用回执重放现有不可变配置，只报告本次要求模型如何写，不保证真实成文效果。

文字层 PDF 与逐章云端识别明确延期：当前提供图像型 PDF、文字文档导出、本地整理及逐条确认；前者尚需字体与文字映射验收，后者尚需完整授权、披露与恢复链。

本轮已获完整本地门禁通过后的安全发布授权；本文件不预填发布成功。完整验证、产物来源、远端发布和清理结果以本轮安装包随附报告及最终交付记录为准。
